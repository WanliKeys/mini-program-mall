# 引流方接入简版说明：生成 URL Link 跳转小程序

## 1. 接口
- `POST https://jxxcfwlkj.cn/api/referral/url-link`
- `Content-Type: application/json`

## 2. 请求参数
- `linkCode` **必填**：我方提供的商品引流码（对应商品/配置）
- `partnerOrderNo` **必填**：引流方订单号，需唯一
- `notifyUrl` **必填**：引流方接收支付结果的回调 URL，需先 URL 编码后再传（示例 `https%3A%2F%2Fexample.com%2Fnotify`）

## 3. 调用示例
```bash
curl -X POST https://jxxcfwlkj.cn/api/referral/url-link \
  -H "Content-Type: application/json" \
  -d '{
    "linkCode":"ABC50",                 // 示例：ABC50 绑定 50 元商品
    "partnerOrderNo": "ORDER20250101-0001",
    "notifyUrl": "https%3A%2F%2Fpartner.com%2Fnotify"
  }'
```

## 4. 成功响应示例
```json
{
  "success": true,
  "data": {
    "urlLink": "https://wxaurl.cn/xxxxxx",
    "path": "pages/order/confirm/confirm",
    "query": "isReferral=true&productId=...&linkCode=ABC50&partnerOrderNo=ORDER20250101-0001&notifyUrl=https%253A%252F%252Fpartner.com%252Fnotify&quantity=1",
    "env": "release"
  }
}
```

## 5. 使用方式
- 直接把 `urlLink` 用作 H5 按钮或生成二维码；用户在任意浏览器点击会唤起微信，进入小程序订单确认页（已携带商品、订单号、回调地址），用户在小程序内点击支付。

## 6. 支付结果回调
- 支付完成后，我方向 `notifyUrl` 发送 `POST`（`application/json`）：
```json
{
  "orderNo": "<partnerOrderNo>",
  "amount": 29.99,
  "status": "paid",          // paid / failed
  "cardCodes": ["CARD123"]   // 虚拟商品有卡密，实物为空数组
}
```
- 请返回 HTTP 200，body 示例：
```json
{ "code": "SUCCESS", "message": "ok" }
```
否则会按重试策略多次推送。

## 7. 注意事项
- `partnerOrderNo` 唯一，避免重复。
- `notifyUrl` 必须可公网访问，建议 HTTPS，调用接口时需先 URL 编码。
- 默认生成的 URL Link 为长期有效（未设置过期）。
