# 后端多进程导致命中旧代码 - 问题复盘与解决指南

## 现象
- 小程序静默登录接口始终返回笼统 500：`{"success":false,"message":"登录服务暂时不可用"}`
- 前端已打印新的 wx.login code；直连微信 `jscode2session` 返回成功（说明服务器出网/证书正常）
- 后端按理应透传微信错误，但前端仍拿到旧文案 → 命中了旧代码进程

## 根因
- 本机同时运行了“多个 wx-backend 进程”（`node src/app.js`），部分进程未包含最新代码（如错误透传/日志增强）。
- 请求被随机命中旧进程，表现为：
  - 仍返回“登录服务暂时不可用”的旧文案
  - 后端控制台没有新增的详细日志

## 复现与定位
1. 列出所有后端进程：
   ```bash
   ps aux | grep "node src/app.js"
   ```
2. 若看到多个 `node src/app.js`，可判定存在遗留实例。
3. 触发 `/api/auth/login`，检查控制台是否打印“登录错误 detail …/微信登录失败(errcode)”；没有即为旧实例。

## 一次性解决（本机）
1. 强杀所有遗留实例：
   ```bash
   pkill -f "/Users/liwanli/Documents/GitHub/mall/wx-backend/src/app.js" || true
   pkill -f "node src/app.js" || true
   ```
2. 仅启动一个新的实例：
   ```bash
   cd /Users/liwanli/Documents/GitHub/mall/wx-backend
   PORT=3000 NODE_ENV=development npm run start
   ```
3. 再次触发登录，确认：
   - 控制台出现“登录错误 detail …”，或
   - 前端 message 透出“微信登录失败(errcode): errmsg”。

## 长期治理建议
- 本地开发尽量单进程前台运行；或使用 `nodemon/tsx watch`，避免多终端各自启动。
- 启动前检查端口占用：`lsof -i :3000`。
- 统一启停脚本：
  ```bash
  # scripts/dev-restart.sh
  pkill -f "/Users/liwanli/Documents/GitHub/mall/wx-backend/src/app.js" || true
  pkill -f "node src/app.js" || true
  cd /Users/liwanli/Documents/GitHub/mall/wx-backend && PORT=3000 NODE_ENV=development npm run start
  ```
- 日志：对登录/支付等关键路径保留可定位日志，避免“吞错”。

## 常见误区
- “直连微信成功，但后端仍 500” → 多半命中旧进程或 `.env` 未生效。
- “我改了代码但行为没变” → 不是代码无效，而是你连到了另一个旧进程。
- “偶发 OK 偶发不 OK” → 负载在多个进程间切换，时好时坏。

## 登录排查清单
- [ ] 前端每次打印新 `code`（不复用）。
- [ ] 仅 1 个后端实例运行。
- [ ] 后端日志有“登录错误 detail …”。
- [ ] 微信报错时 message 透出 `微信登录失败(errcode): errmsg`。
- [ ] `.env` 中 `WECHAT_APPID/WECHAT_SECRET` 为真实值并已重启。

> 按上述步骤可确保“命中新代码”和“错误可见”，显著降低排障成本。
