# 商城引流系统对接文档

## 🎯 系统流程

```
商城后台生成引流链接 → 引流方获取链接 → 用户在引流方系统点击 → 跳转小程序下单支付 → 支付完成通知引流方
```

## 🔗 1. 引流链接说明

### 获取方式

引流方通过商城后台管理系统获取引流链接，包含以下固定参数：

| 参数名 | 说明 | 示例 |
|--------|------|------|
| `productId` | 商品ID（固定，由后台配置） | `123` |
| `linkCode` | 引流链接码（固定，由后台生成） | `ABC123` |

### 引流方动态添加参数

引流方在使用链接时，需要动态添加以下参数：

| 参数名 | 说明 | 示例 |
|--------|------|------|
| `partnerOrderNo` | **必需**：引流方订单号 | `ORDER20241207001` |
| `notifyUrl` | **必需**：支付通知回调URL（需URL编码） | `https%3A//partner.com/notify` |
| `externalOrderNo` | 可选：外部订单号 | `EXT123456` |

### 完整链接示例

**后台生成的基础链接**：
```
https://yourdomain.com/referral-jump.html?productId=123&linkCode=ABC123
```

**引流方添加参数后的完整链接**：
```
https://yourdomain.com/referral-jump.html?productId=123&linkCode=ABC123&partnerOrderNo=ORDER20241207001&notifyUrl=https%3A//partner.com/notify&externalOrderNo=EXT123456
```

## 📢 2. 支付通知回调

### 请求方式

- **HTTP POST** 请求
- **Content-Type**: `application/json`

### 请求体格式

```json
{
  "orderNo": "ORDER20241207001",
  "amount": 29.99,
  "status": "paid",
  "cardCodes": ["CARD123", "CARD456", "CARD789"]
}
```

### 字段说明

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `orderNo` | String | 引流方订单号（与传入的partnerOrderNo一致） |
| `amount` | Number | 支付金额（元） |
| `status` | String | 支付状态：`paid`（成功）、`failed`（失败） |
| `cardCodes` | Array | 卡密数组（虚拟商品才有，实物商品为空数组） |

### 响应要求

**成功响应**：
```json
{
  "code": "SUCCESS",
  "message": "接收成功"
}
```

**失败响应**：
```json
{
  "code": "ERROR",
  "message": "错误信息"
}
```

### 重试机制

- **超时时间**：10秒
- **重试次数**：最多5次
- **重试间隔**：30秒 → 1分钟 → 5分钟 → 15分钟 → 30分钟

## 🛡️ 3. 注意事项

1. **引流链接获取**：通过商城后台管理系统获取基础引流链接
2. **动态参数添加**：引流方需要在基础链接上添加`partnerOrderNo`和`notifyUrl`参数
3. **订单号唯一性**：确保每个`partnerOrderNo`唯一
4. **URL编码**：`notifyUrl`参数需要进行URL编码
5. **卡密处理**：虚拟商品支付成功后会返回卡密数组
6. **响应状态**：必须返回HTTP 200状态码，否则系统会重试
