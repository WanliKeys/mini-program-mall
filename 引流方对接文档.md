# 商城引流系统对接文档

## 🎯 系统流程

```
商城后台生成引流链接 → 引流方获取链接 → 用户在引流方系统点击 → 跳转小程序下单支付 → 支付完成通知引流方
```

## 🔗 1. 引流链接说明

### 获取方式

引流方通过商城后台管理系统获取基础链接模板（由我方落地页承载），包含以下固定参数。**注意：落地页使用公众号 JS-SDK 调起小程序，后台会用服务号的 `WECHAT_H5_APPID/WECHAT_H5_SECRET` 获取 `jsapi_ticket` 生成签名，必须保证公众号已在“JS 接口安全域名”中配置 `jxxcfwlkj.cn`。小程序自身的 AppID/Secret 只用于小程序业务，两者不可混用。**

| 参数名 | 说明 | 示例 |
|--------|------|------|
| `productId` | 商品ID（固定，由后台配置） | `123` |
| `linkCode` | 引流链接码（固定，由后台生成） | `ABC123` |

### 引流方动态添加参数

引流方在使用链接时，需要动态添加以下参数：

| 参数名 | 说明 | 示例 |
|--------|------|------|
| `partnerOrderNo` | **必需**：引流方订单号 | `ORDER20241207001` |
| `notifyUrl` | **必需**：支付通知回调URL（需URL编码） | `https%3A//partner.com/notify` |
| `externalOrderNo` | 可选：外部订单号 | `EXT123456` |

### 完整链接示例

**后台生成的基础落地页链接**：
```
https://jxxcfwlkj.cn/referral-jump.html?productId=123&linkCode=ABC123
```

**引流方添加参数后的模板示例**：
```
https://jxxcfwlkj.cn/referral-jump.html?productId=123&linkCode=ABC123&partnerOrderNo=ORDER20241207001&notifyUrl=https%3A//partner.com/notify&externalOrderNo=EXT123456
```

> ⚠️ 仅有该模板还无法直接跳转小程序，还需要调用签名接口获取最终的 `signedUrl`。

## 🧾 2. 获取带签名的落地页链接

由于微信限制，必须由我们在可信域名上生成 JS-SDK 签名。引流方只需要调用以下接口即可拿到可直接投放的链接：

- **接口地址**：`POST https://jxxcfwlkj.cn/api/referral/signed-link`
- **Content-Type**：`application/json`

### 请求体参数

| 参数名 | 必填 | 说明 |
|--------|------|------|
| `linkCode` | ✅ | 管理后台生成的 `linkCode` |
| `partnerOrderNo` | ✅ | 引流方唯一订单号 |
| `notifyUrl` | ✅ | 引流方接收支付通知的 URL（需 URL 编码后再传） |
| `externalOrderNo` | ❌ | 可选，外部扩展订单号 |
| `env` | ❌ | 跳转目标小程序版本：`release`（默认）、`trial` |

### 响应示例

```json
{
  "success": true,
  "data": {
    "signedUrl": "https://jxxcfwlkj.cn/referral-jump.html?...#signature=xxx&nonceStr=xxx&timestamp=xxx",
    "signature": "xxx",
    "nonceStr": "xxx",
    "timestamp": 1712470000,
    "appId": "wx0b1ce2aa786ec457",
    "linkCode": "ABC123",
    "partnerOrderNo": "ORDER20241207001",
    "notifyUrl": "https://partner.com/notify"
  }
}
```

> `signedUrl` 就是最终用于 H5 引导用户的链接，落地页会在微信内自动拉起小程序支付页。  
> 建议引流方在生成订单时调用该接口并将 `signedUrl` 投放到自己的页面。

## 📢 2. 支付通知回调

### 请求方式

- **HTTP POST** 请求
- **Content-Type**: `application/json`

### 请求体格式

```json
{
  "orderNo": "ORDER20241207001",
  "amount": 29.99,
  "status": "paid",
  "cardCodes": ["CARD123", "CARD456", "CARD789"]
}
```

### 字段说明

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `orderNo` | String | 引流方订单号（与传入的partnerOrderNo一致） |
| `amount` | Number | 支付金额（元） |
| `status` | String | 支付状态：`paid`（成功）、`failed`（失败） |
| `cardCodes` | Array | 卡密数组（虚拟商品才有，实物商品为空数组） |

### 响应要求

**成功响应**：
```json
{
  "code": "SUCCESS",
  "message": "接收成功"
}
```

**失败响应**：
```json
{
  "code": "ERROR",
  "message": "错误信息"
}
```

### 重试机制

- **超时时间**：10秒
- **重试次数**：最多5次
- **重试间隔**：30秒 → 1分钟 → 5分钟 → 15分钟 → 30分钟

## 🛡️ 3. 注意事项

1. **引流链接获取**：通过商城后台管理系统获取基础引流链接
2. **签名接口必调**：要调用 `/api/referral/signed-link` 生成真正可用的 `signedUrl`
3. **动态参数添加**：调用接口时传入 `partnerOrderNo`、`notifyUrl` 等实际参数
4. **订单号唯一性**：确保每个 `partnerOrderNo` 唯一
5. **URL编码**：`notifyUrl` 需先 URL 编码再传
6. **卡密处理**：虚拟商品支付成功后会返回卡密数组
7. **响应状态**：支付通知必须返回 HTTP 200，否则系统会触发重试
