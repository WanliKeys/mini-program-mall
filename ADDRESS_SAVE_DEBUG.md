# 🔍 地址保存问题调试指南

## 🎯 **问题分析**

地址保存后看不到数据，再次进入还是新增地址页面。

## 🔍 **根本原因**

通过后端API测试发现：**认证token无效或缺失**

```bash
# 后端返回 401 错误
{"success":false,"code":401,"message":"认证令牌无效或已过期"}
```

## 🔧 **已添加的调试功能**

### **1. 保存流程日志**
- 请求参数追踪
- 后端返回结果日志
- 网络请求详情

### **2. 地址列表加载日志**  
- 列表加载状态
- 从后端获取的数据
- 数据数量统计

### **3. 登录状态检查**
- Token存在性检查
- 用户信息验证
- 自动静默登录

## 📋 **测试步骤**

### **步骤1: 检查登录状态**
1. 重新编译小程序
2. 进入地址编辑页面
3. 查看控制台输出：
   ```
   当前登录状态: {
     hasToken: true/false,
     token: "eyJ...",
     user: {...}
   }
   ```

### **步骤2: 测试保存流程**
1. 填写地址信息  
2. 点击保存按钮
3. 观察控制台日志：
   ```
   准备发送请求到后端: {...}
   后端返回结果: {...}
   ```

### **步骤3: 检查地址列表**
1. 保存成功后返回地址列表
2. 查看控制台：
   ```
   === 开始加载地址列表 ===
   从后端获取到的地址列表: [...]
   地址数量: 1
   ```

## ⚠️ **可能的问题和解决方案**

### **A. 没有登录token**
- **症状**: `hasToken: false`
- **解决**: 页面会自动调用 `app.silentLogin()`

### **B. token已过期**
- **症状**: 后端返回401错误
- **解决**: 清除存储，重新登录

### **C. 后端API问题**
- **症状**: 网络请求失败
- **解决**: 检查后端服务状态

### **D. 数据库连接问题**
- **症状**: 保存成功但查询不到数据
- **解决**: 检查数据库连接和表结构

## 🚀 **临时解决方案**

如果认证问题持续存在，我们可以：

1. **手动清除token重新登录**
   ```javascript
   wx.removeStorageSync('token');
   wx.removeStorageSync('user');
   ```

2. **使用模拟数据**
   - 前端直接存储到本地
   - 绕过后端API调用

3. **修复后端认证逻辑**
   - 检查JWT secret配置
   - 验证token生成逻辑

## 📊 **期望的正常流程**

```
用户填写地址 
    ↓
点击保存按钮
    ↓  
检查登录状态(有效token)
    ↓
发送POST请求到 /api/addresses
    ↓
后端验证token ✅
    ↓
保存到数据库 ✅
    ↓
返回成功响应
    ↓
前端显示成功提示
    ↓
返回地址列表页面
    ↓
自动加载地址列表 ✅
    ↓
显示刚保存的地址 ✅
```

---

**请按步骤测试并报告控制台日志内容！** 🎯
