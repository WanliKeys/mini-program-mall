# 微信支付接入完整指南

## 📋 接入前准备

### 1. 微信支付商户号申请
1. 访问 [微信支付商户平台](https://pay.weixin.qq.com/)
2. 完成企业认证和商户号申请
3. 获取商户号（MCHID）

### 2. 小程序配置
1. 在微信公众平台绑定商户号
2. 确保小程序已通过微信认证
3. 获取小程序AppID和Secret

## 🔧 配置步骤

### 第一步：配置环境变量

创建 `wx-backend/.env` 文件：

```bash
# 服务配置
PORT=3000
NODE_ENV=development

# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_NAME=mall_db
DB_USER=root
DB_PASSWORD=

# JWT配置
JWT_SECRET=your_super_secret_jwt_key_here_change_this_in_production
JWT_EXPIRES_IN=7d

# 微信小程序配置
WECHAT_APPID=wx1234567890abcdef  # 替换为你的小程序AppID
WECHAT_SECRET=your_wechat_mini_program_secret  # 替换为你的小程序Secret

# 微信支付配置
WECHAT_PAY_MCHID=1234567890  # 替换为你的商户号
WECHAT_PAY_PRIVATE_KEY_PATH=./certs/wechat_pay_private_key.pem
WECHAT_PAY_CERT_SERIAL_NO=your_cert_serial_number  # 替换为证书序列号
WECHAT_PAY_APIV3_KEY=your_apiv3_key_32_chars_long  # 替换为APIv3密钥
WECHAT_PAY_NOTIFY_URL=http://localhost:3000/api/payments/callback/wechat

# 文件上传配置
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=10485760

# 第三方通知配置
THIRD_PARTY_NOTIFY_URL=https://third-party-platform.com/api/notify

# 日志配置
LOG_LEVEL=info
LOG_FILE_PATH=./logs
```

### 第二步：获取微信支付证书

1. **登录微信支付商户平台**
   - 访问 https://pay.weixin.qq.com/
   - 使用商户号登录

2. **下载商户证书**
   - 进入【账户中心】->【API安全】
   - 点击【申请证书】
   - 下载商户API证书（.pem格式）

3. **获取证书序列号**
   - 在【API安全】页面查看证书序列号
   - 复制序列号到环境变量

4. **设置APIv3密钥**
   - 在【API安全】页面设置APIv3密钥
   - 密钥长度必须为32位
   - 复制密钥到环境变量

5. **保存证书文件**
   - 将下载的私钥文件重命名为 `wechat_pay_private_key.pem`
   - 保存到 `wx-backend/certs/` 目录

### 第三步：配置支付回调URL

1. **开发环境**
   - 使用内网穿透工具（如ngrok）
   - 将本地服务暴露到公网
   - 设置回调URL为：`https://your-ngrok-url.ngrok.io/api/payments/callback/wechat`

2. **生产环境**
   - 使用你的域名
   - 设置回调URL为：`https://yourdomain.com/api/payments/callback/wechat`

## 🧪 测试步骤

### 第一步：启动服务

```bash
# 启动后端服务
cd wx-backend
npm run start

# 启动管理后台
cd admin-server
npm run start
```

### 第二步：测试支付流程

1. **创建测试商品**
   - 在管理后台添加商品
   - 设置价格（建议使用0.01元测试）

2. **测试支付**
   - 在小程序中下单
   - 选择微信支付
   - 使用微信开发者工具测试

### 第三步：验证支付回调

1. **检查日志**
   - 查看后端控制台日志
   - 确认支付回调被正确接收

2. **检查数据库**
   - 确认订单状态更新为已完成
   - 确认卡密被正确分配

## 🔍 常见问题排查

### 问题1：支付参数错误
**错误信息：** `微信支付订单创建失败: 参数错误`

**解决方案：**
- 检查AppID是否正确
- 检查商户号是否正确
- 检查证书文件是否存在

### 问题2：签名验证失败
**错误信息：** `签名验证失败`

**解决方案：**
- 检查证书序列号是否正确
- 检查APIv3密钥是否正确
- 检查私钥文件格式

### 问题3：支付回调失败
**错误信息：** 支付成功但订单状态未更新

**解决方案：**
- 检查回调URL是否可访问
- 检查回调URL是否正确配置
- 检查服务器防火墙设置

## 📱 小程序配置

### 1. 更新小程序AppID

在 `wx-frontend/app.js` 中确认AppID配置：

```javascript
globalData: {
  userInfo: null,
  token: null,
  baseURL: 'http://localhost:3000/api',  // 开发环境
  // baseURL: 'https://yourdomain.com/api',  // 生产环境
  version: '1.0.0',
  isLogin: false
}
```

### 2. 配置服务器域名

在微信公众平台配置服务器域名：
- request合法域名：`https://yourdomain.com`
- uploadFile合法域名：`https://yourdomain.com`
- downloadFile合法域名：`https://yourdomain.com`

## 🚀 生产环境部署

### 1. 服务器配置
- 使用HTTPS协议
- 配置SSL证书
- 设置防火墙规则

### 2. 环境变量配置
- 使用生产环境的参数
- 确保证书文件安全
- 设置正确的回调URL

### 3. 监控和日志
- 配置支付日志记录
- 设置异常监控
- 定期检查支付状态

## 📞 技术支持

如果遇到问题，可以：
1. 查看微信支付官方文档
2. 检查微信支付商户平台日志
3. 联系微信支付技术支持

---

**注意：** 请确保在生产环境使用前，充分测试所有支付流程！
